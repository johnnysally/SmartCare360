# Copy to .env and edit
PORT=5000
JWT_SECRET=replace_this_with_a_secure_secret
APIS_URL=http://localhost:4000
DATABASE_FILE=./smartcare360.db
# When deploying to Render or other hosts with Postgres, set DATABASE_URL
# Example: postgres://user:password@host:5432/dbname
DATABASE_URL=
# If your Postgres requires SSL (common on managed providers), set:
PGSSLMODE=require
